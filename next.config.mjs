/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
  // Delete the assetPrefix line entirely so paths default to absolute root mappings
}

export default nextConfig;